import React from 'react';
import TNavbar from '../../Shared/TNavbar';

const Calender = () => {
    return (
        <div>
            <TNavbar />
            Calender
        </div>
    );
};

export default Calender;