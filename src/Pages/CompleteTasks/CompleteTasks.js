import React from 'react';
import TNavbar from '../../Shared/TNavbar';

const CompleteTasks = () => {
    return (
        <div>
            <TNavbar />
            complete tasks
        </div>
    );
};

export default CompleteTasks;